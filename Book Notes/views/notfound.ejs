<%- include('partials/header', { pageTitle: 'Not Found' }) %>

<section class="card">
  <h2>Not Found</h2>
  <% if (id) { %>
    <p>No book with id <code><%= id %></code> was found.</p>
  <% } else { %>
    <p>That page does not exist.</p>
  <% } %>
  <a class="btn" href="/">Go Home</a>
</section>

<%- include('partials/footer') %>
